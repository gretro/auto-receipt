info:
  name: Get all donations
  type: http
  seq: 3

http:
  method: GET
  url: "{{domain}}/listDonations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
