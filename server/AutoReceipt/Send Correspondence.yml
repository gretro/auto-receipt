info:
  name: Send Correspondence
  type: http
  seq: 3

http:
  method: POST
  url: "{{domain}}/sendCorrespondence"
  headers:
    - name: Content-Type
      value: application/json
    - name: Origin
      value: http://localhost:3000
  body:
    type: json
    data: |-
      {
        "toSend": [
          {
            "donationId": "e874df07-c682-405a-9437-c69986a86aaf",
            "correspondenceType": "thank-you"
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
