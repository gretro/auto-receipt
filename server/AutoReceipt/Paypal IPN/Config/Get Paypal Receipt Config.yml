info:
  name: Get Paypal Receipt Config
  type: http
  seq: 1

http:
  method: GET
  url: "{{domain}}/api/paypal/config/:id"
  params:
    - name: id
      value: EVENT-01
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
